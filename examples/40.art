Foo : mut Type = {a: ℕ, b: ℝ}
Bar := Foo | {c: ℕ}

λf (bar: Bar) → ℕ = case bar
	{a: 1, b: 1}              → 1
	{c: 2}                    → 2
	{a, b} | (a + 2) % 2 == 0 → 3
	_                         → 4
