λf (a: ℕ) → ℕ = x := ( g(a) + h(a + 2, 1, 2) + i(1, 2, 3, 4, 5, 6) )

λg (b: ℕ) → ℕ = { b × 4 }

λh (a: ℕ, b: ℕ, c: ℕ, d: ℕ) → ℕ = a + b × c - d

λi (a: ℕ, b: ℕ, c: ℕ, d: ℕ, e: ℕ, f: ℕ) → ℕ = a + b × c - d

λj (x: ℕ) → ℕ = a := b := c := d := x

λk (x: ℕ) → ℕ = {
	y : mut ℕ = 1
	a := y = b := x + 5
}
