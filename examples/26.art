λf (a: ℕ) → ℕ = x := {
	0
	+ g(a)
	+ h(a + 2, 1, 2, 3)
	+ i(1, 2, 3, 4, 5, 6, 7, 8)
	+ j(8)
	+ k(2)
	+ l(1, 2, 3, 4, 5, 6, 7, 8, 9)
}

λg (b: ℕ) → ℕ = { b × 4 }

λh (a: ℕ, b: ℕ, c: ℕ, d: ℕ) → ℕ = a + b × c - d

λi (a: ℕ, b: ℕ, c: ℕ, d: ℕ, e: ℕ, f: ℕ, g: ℕ, h: ℕ) → ℕ = a + b × c - d
λl (a: ℕ, b: ℕ, c: ℕ, d: ℕ, e: ℕ, f: ℕ, g: ℕ, h: ℕ, i: ℕ) → ℕ = a + b × c - d

λj (x: ℕ) → ℕ = a := b := c := d := x

λk (x: ℕ) → ℕ = {
	y : mut ℕ = 1
	a := y = b := x + 5
}
