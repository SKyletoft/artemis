∅ := {}
π := 3.141592653589793
τ := 6.283185307179586

// Iota
λι (n: ℕ) → [ℕ];
// Sum
λΣ (xs: [ℝ]) → ℝ;
// Product
λΠ (xs: [ℝ]) → ℝ;

// Derivative at
λ∂ (f: λ(ℝ) → ℝ, x: ℝ) → ℝ = (
	e := 0.000000001
	(f(x + e) - f(x)) / e
)
